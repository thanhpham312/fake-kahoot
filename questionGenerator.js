const tmdb = require('./tmdb');

var generateDirectorQuestion = (director, answer) => {
    
}

var generateQuestions = (movieList) => {
    questionList = []
}

module.exports = {
    generateQuestions
}