const tmdb = require('./tmdb');

let generateDirectorQuestion = (director, answer) => {
    
};

let generateQuestions = (movieList) => {
    questionList = []
};

module.exports = {
    generateQuestions
};